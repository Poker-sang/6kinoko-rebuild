> 最新内部类型收尾见 [internal-types-20260926](../internal-types-20260926/README.md)：内部指针、已恢复字段和旧转发接口的实现清单已收尾；保留边界与待用户运行验证逐项分列。下文保持历史快照，不再把其中已迁移的整数槽条目当成新一轮 ACT 待办。

> 最新清理交接见 [final-symbol-cleanup-20260926](../final-symbol-cleanup-20260926/README.md)：宿主字段恢复与 268 个历史标识整理；构建和运行状态以该记录为准。下文保留历史快照。

> 最新主宿主迁移交接见 [runtime-host-cpp-20260926](../runtime-host-cpp-20260926/README.md)：源码 `7caa3b8` 完整构建及 DAT 校验完成，运行验证待用户执行。下文保留此前全项目核对快照，不作为本版产物路径或执行结果。

# 全项目剩余清单（2026-09-26）

核对基线：`523f13821219d62095603efe47b306b24469dec1`。本轮仅核对源码、既有原版证据、交接记录和测试源码，未修改游戏逻辑、未构建、未执行游戏或自动测试。本清单覆盖项目主要运行链和迁移边界，不声称逐函数完成语义验收。

## 后续收尾更新

本清单审计之后，[内部边界与失败策略批次](../boundary-closeout-20260926/README.md) 已实现选定指针/字段整理、补齐存档文件往返及独立格式样本测试源码，并恢复纹理创建 HRESULT 与应用初始化失败短路。下面的原审计条目保留用于追溯；这些具体项不再属于“尚未实现”，编译/运行状态以该批 HANDOFF.md 为准。

忽略 UnlockRect 返回值已确认是原版行为，不能继续当作待修复差异。线程/事件/COM 保护等明确保留项见该批逐项台账；未声称全项目所有整数槽均已迁移。

## 判断口径

“已完成”表示下表所列批次范围的实现及证据整理完成，不等于所有异常场景均运行通过。“待运行验证”不能重新计作未实现。“保留边界”包括真实虚调用、文件格式偏移和 Win32 ABI；不能为消除数值或普通指针而改动原版行为。

最新游戏产物源码为 `2684809`（行为修改 `be172e8`），游戏及 63 个 contract EXE 已编译，三个 DAT 已复制并校验。用户随后反馈“没有问题”，记为用户报告正常，未提供逐场景记录；不记为代理冒烟通过或 63 个测试执行通过。前批 `7ca77b1` 亦由用户确认正常。

## 已完成范围：不再整体重开

| 模块 | 已完成范围与依据 | 尚不能据此宣称 |
| --- | --- | --- |
| DAT、脚本加载 | 挂载/读取、脚本入口及失败行为收尾；[加载链记录](../migration/final-loading-chain-20260924.md) | 任意损坏资源均已运行覆盖 |
| ACT 文档、资源、阶段/地图 | 加载失败、借用/所有权、清理链；[ACT 交接](../act-ownership-20260926/HANDOFF.md) | 全部 ACT 字段均已改成具名类型 |
| 对象、脚本、场景 | 创建绑定、每帧更新销毁、切关和重新开始；[对象场景记录](../object-scene-chain-20260926/README.md) | 所有重入、异常回调路径均已实测 |
| 渲染、相机、四边形 | R145–R147 可见查询、相机布局、提交顺序/UV/半像素；[记录](../render-camera-r145-r147/README.md) | 各设备上的像素级一致 |
| 图形设备、渲染目标 | R148–R150 创建回退、丢失/重置、监听与 surface 所有权；[记录](../device-r148-r150/README.md) | 真实设备丢失恢复已验证 |
| 地图 chip 缓存 | R151 原版缓存、变更队列和克隆销毁；[记录](../mcd-r151/README.md) | 原版存在额外的自主动画计时器 |
| 纹理图片 | CV2 读取、像素上传；当前 texture_image.cpp 已有调色板转换与 RLE 解码 | 调色板全部调用路径已逐一验收；失败 HRESULT 完全一致 |
| 输入 | 按键/修饰键、物理设备及输入聚合；[记录](../input-runtime-20260922/README.md) | 控制器、失焦等分支均已实测 |
| 音频 | BGM 加载/退休线程、淡出、环形缓冲与叠加调度；[记录](../audio-scheduling/README.md) | 所有淡入淡出、循环和时序均已听测 |
| 应用生命周期 | 具名状态、场景交接、更新绘制及线程资源退出；[记录](../application-lifecycle/README.md) | 与原版所有启动失败策略完全相同 |
| 文字、网格 | 已有字符串/字形/字体资源批次和 3D 布局恢复；[文字](../text-stage-continuation-20260923/README.md)、[网格](../mesh-3d-layouts/README.md) | 全部内部偏移都已消除 |
| Squirrel 与绑定 | 源码 VM、原生调用和绑定已有实现；[绑定记录](../script-binding-completion/README.md) | 整数槽全部完成类型迁移或支持 x64 |
| 存档表 | 递归读写及文件入口四函数已迁移，保留四个短 ABI 别名；[记录](../savedata-table-20260923/README.md) | 实际存档文件往返已验证 |

上述完成状态依据既有批次及当前源码核对，不能把历史文档里的“下一批”直接搬成今日剩余项。特别是旧渲染记录中的设备重建和 chip 缓存待办，已由后续 R148–R151 完成；旧纹理记录中的 RLE/调色板上传待办也不能原样沿用。

## 确实尚未收尾的工作

| 项目 | 当前证据 | 剩余工作性质 |
| --- | --- | --- |
| 内部指针类型与字段表达 | src/squirrel/act_binding.cpp 仍以 int32_t 表达部分对象/VM/回调，并使用 direct_int_offsets；src/reconstructed/act_texture_io.cpp 仍有 resource+property.offset、layer+offset 访问 | 区分原版属性偏移协议与内部可具名字段，逐边界恢复类型；不重做已完成的 ACT 所有权 |
| 其他反编译遗留分账 | act_document、字符串/字形、容器等仍有词法命中；部分是布局或序列化边界 | 尚无全项目逐候选“必要保留/继续迁移/证据不足”台账；不能把所有命中都认定为错误 |
| 存档文件往返覆盖 | 未在 tests 中找到针对 kinoko_savedata_load_file_entry/save_file_entry 的独立文件往返测试；现有属性 round-trip 不等价 | 缺测试源码覆盖，不是仅等运行，更不是存档读写未实现 |
| 纹理失败返回差异 | texture_image.cpp 忽略 UnlockRect 结果，返回 LockRect 状态；旧纹理记录明确未声称恢复原版失败策略 | 已知未闭合的行为差异，后续须按原版失败分支确定修复，不靠猜测 |
| 应用兼容策略差异 | application-lifecycle 记录缺输入/音频时的降级、线程等待等与原版有差别 | 已知兼容选择需逐项保留依据或恢复原版；不能算已完全等价，也不能统称未实现 |

词法扫描只能提供检查入口。本轮没有对每个候选重新反汇编，因此不提供“全项目还剩 N 个错误函数”或新的周数估计。

## 实现已有，主要待用户运行验证

- 图形设备丢失、Reset、surface 重建和失败恢复。
- 控制器、失焦/恢复及非基本按键输入。
- BGM 淡出退休、重叠播放、循环边界与线程时序。
- 资源加载失败、脚本回调异常、对象退休/场景切换的边界情形。
- 已编译的 contract EXE：编译记录不等于执行结果。
- 存档实际读写兼容性：需先补齐或明确往返验证方法，不能假称已有完整回归仅待点击运行。

用户报告的正常游戏体验是有效验证，但没有详细场景记录，以上专项不能据此自动勾选通过。本轮不要求用户立即测试。

## 证据不足或不属于当前 Win32 DAT 目标

- loose BMP 模式：当前 texture_image.cpp 使用 CV2 路径；属于原版其他模式覆盖问题。未证明是当前三个 DAT 的正常游戏阻塞，不把它算作当前加载链缺失。
- 调色板调用方的端到端覆盖、原生 OOM/无效句柄等极端路径，仍需定向证据；不能仅因上传函数存在就宣布全部等价。
- x64/CV4 宽度解耦、跨平台图形/输入/音频后端、mod 配置和现代化设计属于后续目标。
- D3D9/COM、thiscall、真实虚表、文件格式字段与普通 T* 可以合理保留；存在这些内容不等于当前 Win32 版本未完成。

## 可复核的静态快照

[readability-523f138.json](readability-523f138.json) 由 tools/audit_readability.py 对固定基线生成，覆盖 327 个 src/include C/C++ 文件，排除原版参考、vendor 和 tests。

共 2201 个词法函数候选：307 legacy_marked_candidate、317 abi_adapter_candidate、1577 structured_candidate_unreviewed。最后一个名称是工具分类，不能理解成此前无人审查；307 也不是 307 个功能缺陷。不得据此计算完成率。

高密度检查入口包括 act_binding.cpp（46 个标记候选、172 个偏移命中）、act_texture_io.cpp（15、20）；6kinoko_rebuilt.c、squirrel_legacy_api.cpp 和 squirrel_native_calls.cpp 的别名/适配层需单独分账。历史 source-recovery-review 的 6–12 周估计基于旧快照，不代表当前剩余量。

本次结论：已完成的加载、ACT 所有权和对象场景生命周期应保持关闭；当前剩余以内部类型/字段整理、少数已知兼容差异、证据台账和专项验证为主。没有证据支持再把上述完整模块统一列成下一轮重做项。
