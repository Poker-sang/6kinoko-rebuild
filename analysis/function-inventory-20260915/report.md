# 原版函数入口状态更新（2026-09-15）

## 统计结果

沿用 2026-09-14 的源码分类算法与 4,890 个原版地址入口分母。本报告统计源码形态，不代表功能完成率、正确率或原版行为等价率。

| 类别 | 上次数量 | 本次数量 | 本次占比 | 数量变化 |
|---|---:|---:|---:|---:|
| 保留原反编译函数体 | 2515 | 2513 | 51.39% | -2 |
| 已修改／替换的非空实现 | 822 | 828 | 16.93% | +6 |
| 原版即为简单返回／空函数 | 1516 | 1516 | 31.00% | 0 |
| 简化返回／占位候选，待核对 | 8 | 3 | 0.06% | -5 |
| 未找到同名入口，待核对替代 | 29 | 30 | 0.61% | +1 |
| 合计 | 4890 | 4890 | 100% | 0 |

各项百分比单独四舍五入，显示值之和可能不等于 100%。原版简短入口包含简单返回包装，不等于 1,516 个空功能。

## 全部分类变化及替代关系

| 原版地址 | 上次 → 本次 | 解释 |
|---|---|---|
| 45EC60 | 占位候选 → 非空实现 | 显式 actor 入口使用 R11 已验证运动函数体，保留 C 算术及精度行为。 |
| 48BBA0 | 占位候选 → 非空实现 | 显式接收者调用 Squirrel 2.2.2 C++ SQClosure 析构。 |
| 4992C0 | 占位候选 → 非空实现 | 显式接收者调用 Squirrel 2.2.2 C++ SQClass 析构。 |
| 4A96D0 | 占位候选 → 非空实现 | C++ SquirrelObject 长度包装，保留当前 VM 的 push/getsize/pop。 |
| 4A99F0 | 占位候选 → 非空实现 | C++ 数组反转包装；原版语义是 arrayreverse，不是 clear。 |
| 4A9C60 | 保留函数体 → 非空实现 | 用完整双字对象 pair[2] 接收 VM 值并复制到包装对象，替代反编译的单字临时存储；文本分类变化不单独证明语义正确。 |
| 403E50 | 保留函数体 → 无同名入口 | 已由 kinoko_squirrel_object_delete C++ ABI 适配器替代，虚表 g16 指向新实现；不是新增缺失功能。 |

30 个无同名入口中，本批明确核对了新增的 403E50 替代关系；原有 29 个仍沿用待核对分类，不能直接称为 29 个未实现函数。

## 八个候选的真实进度

七个接收者明确的实现迁移到 C++：489F30、489F50、48BBA0、4992C0、4A96D0、4A99F0、4A9D70。45EC60 继续保留已验证的 C 运动算术。完整 C++ Execute 后端仍关闭。

剩余三个简化入口是 `function_489f30(void)`、`function_489f50(source_ptr)`、`function_4a9d70(void)`。它们已有正确接收者版本，但仍有旧生成调用点未恢复接收者，故兼容空壳继续如实列为候选。本批不能称为“八个全部修完”。下一步需依原版汇编逐一恢复这些调用关系，不能猜测 this 或改名掩盖统计。

相邻 403E50 虚析构恢复了原版 this、条件删除及返回 this 的行为，移除旧版释放全局地址／返回未初始化值的错误。三个无外部引用的旧存档副本被移除，实际存档实现未变。详见 [八入口修复报告](../eight-entry-repair-20260914/report.md)。

## 验证边界与用户反馈

- r5 构建代码提交为 `201c700`；quiet、diag 两版各通过 18 项 CTest，三个原版 DAT 均已复制到 EXE 同目录并核验大小及 SHA256。
- 测试覆盖值所有权、自赋值、析构、VM 栈平衡、长度、数组反转，以及已有石头、升降平台和 BGM 浮点环境回归检查。构建日志与 EXE 哈希见八入口报告及同目录产物。
- 2026-09-15 用户对 r5 反馈“暂时没发现问题”。不扩大解释为全关卡通关、指定场景覆盖或两版交互启动验证。
- R11 石头修复此前获得用户确认；本批保留 BGM 浮点环境恢复和已有升降平台逻辑，不添加关卡特判或速度补丁。
- 本次只更新统计及报告，没有修改运行代码、启动游戏或重新运行测试；旧测试产物全部保留。

## 复现与局限

执行 `python analysis/function-inventory-20260915/inventory.py` 可重算；脚本、逐函数 CSV、JSON 与本报告一同归档。旧基线文件保存在 `../function-inventory-20260914/`。

统计代码快照：`f90eb651d7ef79ceaf7c4fce1eeee312ca3a194d`。

输入 SHA256：

- `src/decompiled/6kinoko.exe.c`：`504D899C97D12700AAD88D88EDBC072F95C600184B684C0BF15BAD7471821014`
- `src/decompiled/6kinoko_rebuilt.c`：`0EA7B89719D18AF094E4EE1F9605F490E35CA82F36356B7D984C505CD5307344`

算法按原版 function_地址匹配函数体，忽略注释和空白，检查所有条件编译变体。它不分析运行可达性、不证明语义等价，也不将跨文件 C++ 替代自动折算成同名实现。外部 CRT／汇编辅助函数不在分母内，所以不能据此推导全项目 C++ 比例或汇编移除率。
