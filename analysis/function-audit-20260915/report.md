# 当前函数覆盖复核

代码提交：76e7a119ee3272f567394e9727b9db2631172994。主源码 SHA256：169504d449922ea2106e3fba0bb070b5f61db683e4ea15d98b4de840dad49b05。

最终统计沿用 analysis/function-inventory-20260915/inventory.py 的算法，复制到本目录运行，保留旧统计文件。最终结果为 summary.json 和 functions.csv；audit.py、functions.json、scan.txt 是初步筛查，不能用作最终分类。初筛未完整处理所有函数声明及条件变体，并把正常转发列为候选，已被既有统计算法取代。

| 源码形态 | 数量 | 比例 |
|---|---:|---:|
| 保留原反编译函数体 | 2512 | 51.37% |
| 已修改／替换的非空实现 | 829 | 16.95% |
| 原版即为简单返回／空函数 | 1516 | 31.00% |
| 已确认改名／跨文件替代 | 30 | 0.61% |
| 简化返回／占位候选 | 3 | 0.06% |

分母是提供的反编译源码中 4890 个 function_地址入口，不是游戏功能数，也不是全部原版真实函数数。IDA 当前识别 3963 个函数（其中 599 个库函数）；两种工具的函数拆分不同。本图不声称行为等价率、运行可达率或开发完成率。各百分比独立四舍五入，总和为 99.99%。原版简单返回还包括转发包装，不能当作缺失功能。未找到同名入口也不能当成未实现，例如 403E50 已由 C++ kinoko_squirrel_object_delete 替代，427950 由 act_clone.cpp 替代。

## 明确仍存在的兼容占位

用户允许优化旧统计后，逐项核对了原来 30 个“无同名入口”：24 个是跨文件 C++／库源码替代，6 个是原文件显式 `_this` 接收者入口。已全部加入 inventory.py 的 replacement_groups，逐函数 CSV 记录替代文件。旧统计的这 30 个不应再作为未知缺失项。保留另列，是为呈现改名迁移，而非声称只有这 30 个函数迁移过。其余 829 个非空修改入口也可能调用 C++。

完整性与正确性仍要区分：本次在给定地址入口范围内未发现无法定位替代的缺失入口，但 3 个旧空壳仍在，也有大量未经逐个原版行为比对的函数。保留反编译文本不能证明已正确还原；非空修改也不能证明已修完。

- function_489f30，主源码 184720 行：旧无接收者释放入口 return 0。
- function_489f50，184714 行：旧赋值入口直接返回 source_ptr。
- function_4a9d70，224779 行：旧 SquirrelObject 析构入口 return 0。

三者均已有显式接收者实现，尚存旧生成调用者。不能由此推断正常游戏必定触发，也不能称为已经完整恢复。最近 remaining-three-20260915/report.md 仅修复 GETVARGV、Clone、FOREACH 的一批调用点。

retdec_asm_stubs.h 还列出 58 个汇编辅助函数，retdec_asm_stubs.c 用统一 return 0 实现；包括浮点/SIMD 与 int3 等不同类别。这些是反编译辅助符号，不加入原版地址入口分母。需结合链接剔除、替代实现和运行可达性进一步审查。

## 原版证据

通过技能 start/open 脚本在临时副本打开原版，HTTP IDA MCP 会话 6cfe23cf；预注册工具通道无法访问同一会话，改用仓库 tools/ida_query.ps1 调用同一 MCP 服务。ida-survey.json 含原版 SHA256、PE32 信息、导入分类和函数数量。E-imports：文件 IO、Win32 窗口、D3D9/D3DX、计时和 COM 等导入已记录；没有以导入表推断全部动态能力。

original-489f30.json 显示原版检查引用计数并调用 Release；original-489f50.json 显示原版复制对象双字、增加新引用并释放旧引用。均非当前旧兼容入口的简单返回。辅助核对提供的 ../squirrel-2.2.2/SQUIRREL2/squirrel/sqobject.h 的析构/赋值，以及 analysis/evidence/raw/phase3-20260906/squirrel-sqvm.asm 第 12941 行起 SQVM::Push 的显式 ECX、对象复制和引用计数反汇编。

本次只做静态审计和图表，没有修改游戏实现、构建或运行游戏。未覆盖全关卡、全部功能或所有错误路径。
